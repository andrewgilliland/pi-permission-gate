The permission gate blocked this command:

{{command}}

Analyze the block:
1. **Was blocking the right call?** — explain what damage this command could have caused
2. **What was the agent trying to do?** — infer the likely intent from the command
3. **Safe alternative** — provide a safer command or approach that achieves the same goal without the risk
4. **Gate config recommendation** — should this pattern stay in gate-config.json, be made more specific, or be removed?
